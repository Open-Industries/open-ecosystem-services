openapi: 3.1.0
info:
  title: Open Ecosystem Services Reference API
  version: 0.1.0
  description: Provider-neutral research contract for shared identity, consent, audit, catalog, policy and observability services.
servers:
  - url: https://api.example.org/open-ecosystem-services/v1
paths:
  /ecosystem-services/{id}:
    get:
      operationId: getEcosystemService
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string }
        - name: X-Correlation-ID
          in: header
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Domain resource
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DomainResource'
        '404': { description: Not found }
components:
  schemas:
    DomainResource:
      type: object
      required: [id, schema_version, status, updated_at]
      properties:
        id: { type: string }
        schema_version: { type: string, const: '1.0' }
        status: { type: string }
        updated_at: { type: string, format: date-time }
        metadata:
          type: object
          additionalProperties: true
