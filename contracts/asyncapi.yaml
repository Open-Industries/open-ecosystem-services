asyncapi: 3.0.0
info:
  title: Open Ecosystem Services Reference Events
  version: 0.1.0
channels:
  domainEvents:
    address: open-ecosystem-services.domain-events.v1
    messages:
      domainEvent:
        $ref: '#/components/messages/DomainEvent'
operations:
  receiveDomainEvent:
    action: receive
    channel:
      $ref: '#/channels/domainEvents'
components:
  messages:
    DomainEvent:
      name: EcosystemServiceUpdated
      contentType: application/cloudevents+json
      payload:
        $ref: './cloudevents/domain-event.schema.json'
