# Services

Add independently deployable Open Ecosystem Services domain services here. Each service must own its state, expose versioned contracts, emit correlated telemetry and document idempotency and compensation behavior.
